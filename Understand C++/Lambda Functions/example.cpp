#include <stdio.h>
#include <cstring>
#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main()
{
 // []: capture clause
 // (): used to pass parameters
 // {}: function definition
 // for each iterates through vector
 // for_each(v.begin(), v.end(), something);
 // [capture clause](parameters) {function definition};
vector<int> v {2, 3, 7, 14, 23};
// for each is a function in the algorithm header file

for_each(v.begin(), v.end(), )
}